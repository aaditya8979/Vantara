# VANTARA Frontend Environment Variables
# Copy this file to .env to use local overrides

# Define the base URL for API requests.
# If not set, it defaults to "/api" (which works seamlessly in production via Vercel routing).
# For local development where the FastAPI server is running on port 8000:
# VITE_API_BASE=http://localhost:8000/api
